---
paths:
  - "src/server/followUps/**"
  - "app/api/inspections/**"
  - "app/api/follow-ups/**"
  - "components/Findings.tsx"
---

# Follow up rules

A follow-up belongs to an inspection and one of its finding IDs. Trim notes before validation. Save once before confirming. Keep one open follow-up per finding. Resolution never changes an inspection or report.

These instructions guide work; checks verify the result.
