---
paths:
  - "app/page.tsx"
  - "app/api/vehicles/**"
  - "src/server/vehicles/**"
---

# Search rules

Trim queries. Match make, model and stock number without regard to case. Sort mileage numerically. Preserve saved order for unknown sorts. Join by record identity; never assume arrays align.

These instructions guide work; checks verify the result.
