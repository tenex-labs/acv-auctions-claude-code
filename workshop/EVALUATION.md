# Evaluation record

## Application checks

Record commands actually run, results, dates and saved output. Separate source inspection, supplied teaching records and executed behavior.

## Unchanged regression test

In a disposable copy record correct pass → failure at the intended duplicate 409 assertion → restored pass. Preserve the same test hash across all three. Rebuild and restart changed production code each time. A startup failure or timeout is not defect detection.

## Skill review cases

Use .claude/skills/spec-to-build/evals/README.md. Run all four fixed cases separately with the same model, effort and read-only tools. Save actual outputs and case/skill hashes. Compare with expectations only after reviewing. Keep missed defects, false alarms, unsupported claims and worse results.

Propose one instruction change only when actual evidence justifies it. After user review, repeat the same cases. If no change is justified, record that; do not invent a before/after result.

## Hook evidence

Record healthy, type-error, exactly-500-line, 501-line and irrelevant-Markdown probe output separately from a real matching Edit/Write event. Keep the actual feedback and restoration result.

## Full-workflow replay

The separate definition is .claude/skills/spec-to-build/evals/full-workflow-replay.md. Record intake, prepared answers, waits, review preference, hook setup, implementation and acceptance checks. A packet review does not establish that this full workflow ran.

No observations have been recorded in this template.
