import {test,expect} from '@playwright/test';
test('FU-03 FU-05 FU-06 browser validation, save, reload and resolve',async({page,request})=>{const list=await (await request.get('/api/inspections/insp-004/follow-ups')).json();for(const f of list.followUps)if(f.status==='open')await request.post('/api/follow-ups/'+f.id+'/resolve');await page.goto('/inspections/insp-004');const region=page.getByTestId('finding-finding-006');await region.getByRole('button',{name:'Flag for follow-up'}).click();await region.getByRole('button',{name:'Save follow-up'}).click();await expect(region.getByRole('alert')).toHaveText('Add a note before saving.');await region.getByTestId('follow-up-note').fill('Browser persistence');await region.getByRole('button',{name:'Save follow-up'}).click();await expect(page.getByTestId('follow-up-status')).toHaveText('Follow-up saved.');await page.reload();await expect(region.getByTestId('follow-up-note-text').last()).toHaveText('Browser persistence');await region.getByRole('button',{name:'Mark resolved'}).click();await expect(page.getByTestId('follow-up-status')).toHaveText('Follow-up resolved.');await expect(region.getByTestId('follow-up-resolved').last()).toHaveText('Resolved');});
