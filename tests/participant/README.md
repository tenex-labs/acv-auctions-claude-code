# Participant regression tests

Part 1: author a regression for lowercase stock-number search and prove pass, intended failure and restored pass without changing that test.

Part 2: ask the product questions first. Build “Clarify the feature request” supplies a separate Comparison requirements pack after the discussion. Its installer adds the Task 2 contract, checks and three participant regression placeholders. It does not include a feature implementation. Read the installed COMPARISON-REGRESSION.md before writing the tests.
